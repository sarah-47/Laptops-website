
<ul class="nav navbar-nav navbar-right">
    <li>
        @if (Session["CartCounter"] == null)
        {
            @Html.ActionLink("Cart", "ShopppingCart", "Customer", new { id = "cartItem" })
        }
        else
        {
            @Html.ActionLink("Cart(" + Session["CartCounter"] + ")", "ShopppingCart", "Customer", new { id = "cartItem" })


        }
    </li>
</ul>
